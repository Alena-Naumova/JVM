1.	class JvmComprehension подгружаются через Application ClassLoader находит нужный класс на уровне приложения, проходит через Platform ClassLoader и через Bootstrap ClassLoader класс загружается. После загрузки JVM проверяет класс на валидность, происходит подготовка примитивов в статических полях и разрешение символьных ссылок. Затем происходит инициализация класса и загрузка в область памяти MetaSpace.
 
2.	Метод main подгружается в стек. Переменная туда же.

3.	Object создается в области памяти куче, а ссылка на О помещается в стеке.

4.	Класс Integer проходит через те же процессы, что в п.1 описаны. В стек подгружаются методы класса, в стек подгружается int ii (не уверена).

5.	Вызывается метод printAll.

Создается фрейм и выводится "finished"


В стеке создается пространство для метода printAll в него подгружается ссылка на Object из кучи, берется значение i и ii. С Integer uselessVar происходит то же, что и в п. 4. Создается фрейм, куда подгружаются(??) I и ii
Подгружается метод toString и ссылка на Object o. Суммируются значения всех переменных.

Код выполняется строка за строкой, методы компилируеются и переводится в байткод, а затем через jvm байткод переводятся в машинный код, который понимает компьютер. 






